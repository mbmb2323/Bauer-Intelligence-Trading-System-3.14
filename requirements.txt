# ============================================================
# Bauer Intelligence Trading System — ML Stock Screener
# Requirements for NVIDIA Jetson Orin Nano 8GB
#
# Install notes:
#   PyTorch for Jetson must be installed from NVIDIA's wheel index:
#   https://developer.download.nvidia.com/compute/redist/jp/
#   See README.md for the exact install commands.
# ============================================================

# --- Core numerical / data ---
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scipy>=1.11

# --- Market data ---
yfinance>=0.2.37
requests>=2.31
aiohttp>=3.9          # async download support

# --- Technical analysis ---
pandas-ta>=0.3.14b    # vectorized technical indicators (GPU-friendly via pandas)
ta>=0.11              # fallback pure-Python indicators

# --- Machine learning (CPU-side) ---
lightgbm>=4.3         # gradient boosting for technical signal scoring
scikit-learn>=1.4     # preprocessing, metrics, utilities
joblib>=1.3           # model serialization / parallel jobs

# --- Deep learning (GPU-side) ---
# PyTorch is installed separately from NVIDIA's Jetson wheel index.
# Uncomment the line below ONLY on non-Jetson (x86) development machines:
# torch>=2.2,<3.0

# --- TensorRT Python bindings ---
# TensorRT is bundled with JetPack and exposed via the system Python.
# On Jetson these packages are pre-installed; no pip install needed.
# On x86 dev machines install via:
#   pip install tensorrt
# tensorrt  # uncomment for x86 dev

# --- ONNX (intermediate format for TRT export) ---
onnx>=1.16
onnxruntime>=1.18     # CPU fallback when TRT is unavailable

# --- Visualization / output ---
rich>=13.7            # beautiful terminal tables and progress bars
tabulate>=0.9

# --- Configuration ---
pyyaml>=6.0
python-dotenv>=1.0    # optional .env support for API keys

# --- Utilities ---
tqdm>=4.66
click>=8.1            # CLI interface
coloredlogs>=15.0

# --- Testing ---
pytest>=8.0
pytest-asyncio>=0.23
